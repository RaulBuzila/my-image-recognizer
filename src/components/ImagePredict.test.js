// import React from "react";
// import { expect } from 'chai';
// import { shallow } from "enzyme";

// import ImagePredict from "./ImagePredict";

// const mocks = {
//     error: "This is an error"
// };

// describe('<Image Predict />',  () => {
//     it('should render the Image Predict component', () => {
//         const wrappper = shallow(<ImagePredict error={mocks.error} />);
        
//         expect(wrappper.find("p")).to.have.length(1);
//     })
// })